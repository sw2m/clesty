openapi: 3.0.3
info:
  title: deprecated query
  version: 0.0.1
paths:
  /pets:
    get:
      operationId: listPets
      parameters:
        - name: legacy
          in: query
          deprecated: true
          schema:
            type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
