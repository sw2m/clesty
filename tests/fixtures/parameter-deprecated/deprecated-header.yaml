openapi: 3.0.3
info:
  title: deprecated header
  version: 0.0.1
paths:
  /pets:
    get:
      operationId: listPets
      parameters:
        - name: X-Old
          in: header
          deprecated: true
          schema:
            type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
