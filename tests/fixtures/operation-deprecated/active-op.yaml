openapi: 3.0.3
info:
  title: active op
  version: 0.0.1
paths:
  /thing:
    get:
      operationId: getThing
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
