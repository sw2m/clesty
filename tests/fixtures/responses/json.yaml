openapi: 3.1.0
info:
  title: JSON Response
  version: 0.0.1
paths:
  /thing:
    get:
      operationId: getThing
      responses:
        "200":
          description: JSON body
          content:
            application/json:
              schema:
                type: object
                properties:
                  a:
                    type: integer
