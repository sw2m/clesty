openapi: 3.1.0
info:
  title: Text Response
  version: 0.0.1
paths:
  /thing:
    get:
      operationId: getThing
      responses:
        "200":
          description: Plain text
          content:
            text/plain:
              schema:
                type: string
