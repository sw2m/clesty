openapi: 3.1.0
info:
  title: 200 Only
  version: 0.0.1
paths:
  /thing:
    get:
      operationId: getThing
      responses:
        "200":
          description: only success declared
          content:
            application/json:
              schema:
                type: object
