openapi: 3.1.0
info:
  title: Default Only
  version: 0.0.1
paths:
  /thing:
    get:
      operationId: getThing
      responses:
        default:
          description: only fallback declared
          content:
            application/json:
              schema:
                type: object
