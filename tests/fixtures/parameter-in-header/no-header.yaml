openapi: 3.0.3
info:
  title: no header
  version: 0.0.1
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
