openapi: 3.0.3
info:
  title: required header
  version: 0.0.1
paths:
  /tenants:
    get:
      operationId: listTenants
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
