openapi: 3.0.3
info:
  title: broken local ref
  version: 0.0.1
paths:
  /pets:
    get:
      operationId: listPets
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DoesNotExist"
components:
  schemas:
    Pet:
      type: object
      properties:
        id: { type: integer }
